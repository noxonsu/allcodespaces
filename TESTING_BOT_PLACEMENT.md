# Инструкция по тестированию размещения через бота

## Предварительные условия

1. ✅ Канал создан в админке
2. ✅ Бот добавлен в канал как администратор
3. ✅ У канала установлены доступные слоты публикации (8:00-21:00 по умолчанию)
4. ✅ Создан ChannelAdmin с tg_id
5. ✅ ChannelAdmin привязан к каналу

## Запуск автоматических тестов

```bash
# Запустить все тесты размещения
cd web_app
python3 manage.py test core.tests.test_bot_placement -v 2

# Запустить конкретный тест
python3 manage.py test core.tests.test_bot_placement.BotPlacementTestCase.test_create_campaign_channel_with_slot -v 2
```

## Ручное тестирование

### 1. Подготовка тестовых данных

#### 1.1. Создать креатив (Message)
```
Админка -> Креативы -> Добавить
- Название: "Тестовая реклама"
- Тело: "Текст рекламного сообщения"
- Кнопка: "Перейти"
- Ссылка: "https://example.com"
- Формат: "Фикс-слот" (для теста со слотами)
```

#### 1.2. Проверить канал
```
Админка -> Каналы -> Выбрать канал
Проверить:
- Бот установлен: ✅
- Статус: Подтверждено
- CPM: указан (например, 100)
- Поддерживаемые форматы: включает "Фикс-слот"
```

#### 1.3. Проверить слоты публикации
```
Админка -> Каналы -> Выбрать канал -> Таб "Доступные слоты"
Ссылка: https://telewin.wpmix.net/core/channel/[ID]/?tab=доступные-слоты-tab

Проверить:
- Должна быть визуальная сетка 7x24
- По умолчанию выбраны слоты с 8:00 до 21:00
- Можно кликать на ячейки для выбора/снятия
```

### 2. Создание кампании с фикс-слотом

#### 2.1. Создать кампанию
```
Админка -> Кампании -> Добавить
- Название: "Тестовая кампания"
- Формат: "Фикс-слот"
- Бюджет: 10000
- Дата старта: завтра
- Дата завершения: завтра + 7 дней
- Дата и время публикации: завтра в 10:00
- Креатив: выбрать созданный креатив
- Статус: "На паузе" (пока)
```

#### 2.2. Добавить канал в кампанию
```
В форме кампании -> Секция "Каналы" -> Добавить строку:
- Канал: выбрать тестовый канал
- Админ: выбрать ChannelAdmin
- Слот публикации: выбрать "Понедельник 10:00-11:00" (или нужный день)
- CPM: 100
- План. CPM: 100
- План. Количество показов: 10000
```

#### 2.3. Сохранить кампанию
```
Нажать "Сохранить"
Проверить:
- Кампания сохранена без ошибок
- В списке каналов появилась запись
- Статус публикации: "Ожидает подтверждения"
```

### 3. Проверка уведомления в боте

#### 3.1. Активировать кампанию
```
Админка -> Кампании -> Выбрать кампанию
Изменить статус на "Активна"
Сохранить
```

#### 3.2. Проверить уведомление в Telegram
```
В Telegram боте администратор канала должен получить сообщение:

"Получен запрос на публикацию рекламного сообщения в вашем канале.
Рекламодатель: [Клиент], Бренд: [Бренд], Формат: Фикс-слот
Публикация: [дата время], План. CPM 100, Слот: Понедельник 10:00-11:00"

С кнопками:
[Разрешить 👍] [Отклонить ⛔]
```

### 4. Тестирование публикации

#### 4.1. Одобрить размещение
```
В Telegram боте нажать "Разрешить 👍"
```

#### 4.2. Проверить статус
```
Админка -> Кампании -> Выбрать кампанию -> Таб "Каналы"
Проверить:
- Статус публикации: "Подтверждено"
```

#### 4.3. Дождаться времени публикации
```
Если указанное время публикации еще не наступило - дождаться его
Или изменить время публикации на ближайшее (например, через 2 минуты)
```

#### 4.4. Проверить публикацию
```
1. В Telegram канале должно появиться сообщение:
   - Текст креатива
   - Кнопка с ссылкой
   - Изображение/видео (если было)

2. В админке проверить:
   - Статус публикации: "Опубликовано"
   - Дата публикации: заполнена
   - ID поста в канале: заполнен
```

## Проверка разных форматов

### Фикс-слот (Fixed Slot)
```
Требования:
✅ Обязательно указать "Дата и время публикации"
✅ Обязательно выбрать "Слот публикации"
✅ Время публикации должно попадать в выбранный слот
```

### Автопилот (Autopilot)
```
Требования:
✅ НЕ указывать "Дата и время публикации"
✅ НЕ выбирать "Слот публикации"
✅ Публикация происходит автоматически
```

### Спонсорство (Sponsorship)
```
Требования:
✅ Текст до 160 символов
✅ Максимум 1 кнопка
✅ НЕ указывать "Дата и время публикации"
```

## API эндпоинты для тестирования

### Отправка кампании в бот (для внутреннего использования)
```bash
curl -X POST https://telewin.wpmix.net/telegram/public-campaign-channel \
  -H "Content-Type: application/json" \
  -d '{
    "id": "campaign-channel-uuid",
    "channel": {
      "tg_id": "987654321",
      "name": "Test Channel"
    },
    "channel_admin": {
      "tg_id": "123456789"
    },
    "campaign": {
      "client": "Test Client",
      "brand": "Test Brand",
      "format": "fixed_slot",
      "format_display": "Фикс-слот",
      "message": {
        "body": "Test message",
        "button": {
          "title": "Click",
          "url": "https://example.com"
        }
      }
    },
    "plan_cpm": "100.00",
    "publication_slot": {
      "weekday": "Понедельник",
      "start_time": "10:00",
      "end_time": "11:00"
    },
    "message_publish_date": "2025-10-29T10:00:00Z"
  }'
```

## Проверка логов

### Backend логи
```bash
# Docker логи web-app
docker logs web-app_con --tail 100 -f

# Поиск ошибок
docker logs web-app_con | grep -i error
```

### Bot логи
```bash
# Docker логи бота
docker logs bot_con --tail 100 -f

# Поиск ошибок публикации
docker logs bot_con | grep -i "PUBLISH ERROR"
```

## Частые проблемы

### 1. "Нельзя создавать пересекающиеся временные слоты"
**Решение:** Проверить что слоты не пересекаются. Смежные слоты (8:00-9:00 и 9:00-10:00) допустимы.

### 2. "Время кампании должно попадать в выбранный слот"
**Решение:** Убедиться что время публикации кампании находится между start_time и end_time слота.

### 3. Сообщение не публикуется в канале
**Проверить:**
- Бот добавлен в канал как администратор
- Статус кампании: "Активна"
- Статус CampaignChannel: "Подтверждено"
- Время публикации наступило
- В логах бота нет ошибок

### 4. ChannelAdmin не получает уведомление
**Проверить:**
- ChannelAdmin.is_bot_installed = True
- ChannelAdmin.tg_id корректный
- ChannelAdmin привязан к каналу через ManyToMany
- Бот запущен

## Мониторинг

### Проверка статуса бота
```bash
# Проверить что контейнер бота работает
docker ps | grep bot

# Проверить webhook
curl https://telewin.wpmix.net/telegram
```

### Проверка статуса публикаций
```sql
-- Посмотреть все CampaignChannel со статусами
SELECT
    cc.id,
    ch.name as channel_name,
    c.name as campaign_name,
    cc.publish_status,
    cc.message_publish_date,
    ps.weekday,
    ps.start_time,
    ps.end_time
FROM core_campaignchannel cc
JOIN core_channel ch ON cc.channel_id = ch.id
JOIN core_campaign c ON cc.campaign_id = c.id
LEFT JOIN core_channelpublicationslot ps ON cc.publication_slot_id = ps.id
ORDER BY cc.created_at DESC
LIMIT 10;
```

## Успешное прохождение теста

✅ Автоматические тесты проходят без ошибок
✅ Слоты создаются автоматически при создании канала
✅ Кампания создается со слотом без ошибок
✅ Администратор получает уведомление в боте
✅ Слот отображается в уведомлении
✅ После одобрения статус меняется на "Подтверждено"
✅ Сообщение публикуется в канал в указанное время
✅ Статус меняется на "Опубликовано"
