[pytest]
# Use asyncio mode for all tests
asyncio_mode = auto
# Add 'app' to pythonpath so that tests can import from 'app' directly
pythonpath = . app
# Define environment variables for tests if needed, e.g., to use a test database
# For now, we'll use the same .env, but this is where you'd override for tests
# env = 
#   TEST_DATABASE_URL=sqlite:///./test.db 
#   SETTING_OVERRIDE=test_value
